@model IEnumerable<EntityFrameWorkDemo.Models.Student>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Display</title>
    @Styles.Render("~/Content/css")
</head>
<body>
    <div style="width:600px" class="well">
        <h4>Student</h4>
        <hr />
        <table class="table table-bordered">

            <tr>
                <th>StudentName</th>
                <th>StudentClass</th>
                <th>City</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
            @foreach (var v in Model)
            {
                <tr>
                    <td>@v.StudentName</td>
                    <td>@v.StudentClass</td>
                    <td>@v.City</td>
                    <td>@Html.ActionLink("Edit", "Edit", new { v.StudentName }, new { @class = "btn btn-primary" })</td>
                    @*<td>
                        @using (Html.BeginForm("Delete", "Home"))
                        {
                            @Html.Hidden("StudentName", v.StudentName)
                            <input type="submit"
                                   class="btn btn-default btn-xs"
                                   value="Delete" />
                        }
                        </td>*@
                    <td>@Html.ActionLink("Delete", "Delete", new { v.StudentName },new { @class="btn btn-primary"})</td>

                 </tr>
                
               }

                    </table>
                </div>
                <p>

                    @Html.ActionLink("Back to List", "Index")
                </p>
            </body>
        </html>
